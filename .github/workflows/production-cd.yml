name: production CD for API QAnightly

on:
  release:
    types: [released]

env:
  DISTRIBUTION_APP: "api-qanightly"

jobs:
  production_deployment:
    name: Deploy (production)
    runs-on: ubuntu-latest
    concurrency: production-cd

    steps:
      - name: Checkout the repository 🎁
        uses: actions/checkout@v3

      - name: Authenticating to GCP ⚙️
        uses: google-github-actions/auth@v1
        with:
          project_id: ${{ secrets.GCLOUD_NAMESPACE_PRODUCTION }}
          credentials_json: ${{ secrets.PRODUCTION_GOOGLE_APPLICATION_CREDENTIALS }}

      - name: Get branch name
        id: branch
        uses: tj-actions/branch-names@v5.1

      - name: Trigger build
        uses: aurelien-baudet/workflow-dispatch@v2
        with:
          workflow: production-cd-build-api-qanightly.yml
          repo: PrestaShopCorp/qanightlyresults-cd
          inputs: '{ "branch": "${{ steps.branch.outputs.current_branch }}"}'
          token: ${{ secrets.TERRAFORM_ACCESS_TOKEN }}
          ref: 'refs/heads/main'

      - name: Trigger deploy
        uses: aurelien-baudet/workflow-dispatch@v2
        with:
          workflow: production-cd-deploy-api-qanightly.yml
          repo: PrestaShopCorp/qanightlyresults-cd
          inputs: '{ "branch": "${{ steps.branch.outputs.current_branch }}"}'
          token: ${{ secrets.TERRAFORM_ACCESS_TOKEN }}
          ref: 'refs/heads/main'
